// * Variables ----------------------------------------------------
export let totalGridSize = 20;
export let snakeSpeed = () => 320;

export let initialSnakePosition = [
  { x: totalGridSize / 2, y: totalGridSize / 2 },
  { x: totalGridSize / 2 + 1, y: totalGridSize / 2 },
];
